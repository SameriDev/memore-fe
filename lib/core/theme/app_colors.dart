import 'package:flutter/material.dart';

class AppColors {
  // Memore Brown/Tan Theme Colors
  static const Color primary = Color(
    0xFF8B4513,
  ); // SaddleBrown - main brand color
  static const Color secondary = Color(0xFFCD853F); // Peru - accent gold
  static const Color background = Color(0xFFF5F5DC); // Beige - warm background
  static const Color surface = Color(0xFFF5DEB3); // Wheat - light tan
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color text = Color(0xFF3E2723); // Dark brown for main text
  static const Color textSecondary = Color(
    0xFF6D4C41,
  ); // Medium brown for secondary text
  static const Color divider = Color(0xFFE0E0E0);

  // Additional colors for UI elements
  static const Color cardBackground = Color(
    0xFFFFFFF8,
  ); // Slightly warm white for cards
  static const Color iconColor = Color(0xFF5D4037);
  static const Color shadowColor = Color(0x1A000000);

  // Bottom nav specific colors
  static const Color bottomNavBackground = Color(
    0xFFFAF8F5,
  ); // Slightly warmer white
  static const Color cameraButtonActive = Color(
    0xFFD4AF37,
  ); // Gold for active camera

  // Dark colors
  static const Color darkBackground = Color(
    0xFF000000,
  ); // Black background for buttons

  // Light colors
  static const Color lightBackground = Color(
    0xFFF5F1EB,
  ); // Light beige for screen background
}
